import { BASE_URL } from '~/utils/constant'

export const eventImageUrl = (img) => `${BASE_URL}/storage/uploads/events/${img}`
export const featImageUrl = (img) => `${BASE_URL}/storage/uploads/feat/${img}`
